# Staff System

## Build Plugins
```bash
./build-plugins.sh   # Linux/Mac
build-plugins.bat    # Windows
```

## Setup Permissions
Download LuckPerms, restart server, then run commands from `plugins/LuckPerms/setup-permissions.txt`

## Assign Staff
```
lp user <name> parent set moderator|administrator|eventstaff|founder
```
